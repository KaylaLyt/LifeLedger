## 1. 项目骨架与数据模型

- [x] 1.1 初始化 Android 应用基础结构，并接入 Room、序列化和本地通知所需依赖
- [x] 1.2 定义账户、资产快照、快照余额、花销分类汇总、人员、礼物记录、Todo、循环规则和应用锁设置的数据实体
- [x] 1.3 建立 DAO、Repository 和基础迁移策略，确保快照按 `snapshotDate` 可查询和重算

## 2. 资产快照能力

- [x] 2.1 实现账户档案管理，包括完整号码保存、纳入总资产标记和详情页复制入口
- [x] 2.2 实现资产快照录入界面，支持默认当前时间和手动补录历史日期
- [x] 2.3 实现快照中的账户余额、负债、目标总资产、下次记录时间和备注保存逻辑
- [x] 2.4 实现周期花销分类汇总录入与校验，限制自定义分类最多 10 个
- [x] 2.5 实现总资产变化计算和账户级对比入口，并确保负债不自动并入总资产
- [x] 2.6 实现目标达成判断逻辑，按“总资产大于等于目标值”给出状态

## 3. 人情往来与轻量 Todo

- [x] 3.1 实现人员资料管理和送礼台账录入，支持送出/收到两种方向
- [x] 3.2 实现轻量 Todo 的创建、完成、重开和基本信息展示
- [x] 3.3 实现 Todo 的常见循环规则配置，覆盖按天、按周、按月等常见模式
- [x] 3.4 实现高级循环规则表单，并生成内部可计算的重复规则与下一次触发时间预览
- [x] 3.5 实现生日前两周自动生成“准备礼物”Todo 的联动逻辑，并避免同一生日周期重复生成
- [x] 3.6 实现 Todo 本地提醒调度，覆盖单次和循环任务

## 4. 安全与导出

- [x] 4.1 实现应用级密码锁、正确解锁重置失败次数和错误次数累计逻辑
- [x] 4.2 实现密码失败剩余次数提示，并在第 4 次失败后显示“再失败 1 次将销毁本地数据”的明确警告
- [x] 4.3 实现第 5 次失败后的应用内数据销毁流程，确保不影响已导出的外部 JSON 文件
- [x] 4.4 实现单文件 JSON 导出，输出 `schemaVersion`、`exportedAt`、`accounts`、`snapshots`、`people`、`giftRecords` 和 `todos`

## 5. 验证与收尾

- [x] 5.1 为快照排序、目标达成、分类上限、生日派生 Todo 和密码失败销毁编写测试
- [ ] 5.2 手动验证补录历史快照后的总资产变化、生日待办生成和高级循环规则提醒行为
- [x] 5.3 整理导出示例与关键交互文案，确认敏感信息展示、警告提示和空状态体验
